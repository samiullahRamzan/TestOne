import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    carousel: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '50%',
    },
});